# data / model weights / experiment output — never committed
data/
data_mirrors/
qwen3vl4b/
out/
out*/
chunks/
chunks*/
eval_videos/
eval_flat/
model/adapter_model.safetensors
model/*.pt
model/*.pth

# archived exploratory scripts, old submissions, cached probabilities
# (kept on disk, not tracked)
experiments/
best_checkpoint_preds.json
d1_dense*.json
motion.json
eval/

# run artifacts
*.log
*.jsonl
submission*.json
!examples/submission_template.json
cache_*.json
chunk_cache*.json
val_*.json

# python
__pycache__/
*.pyc

# webapp runtime
webapp/uploads/
webapp/__pycache__/
